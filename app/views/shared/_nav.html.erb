<nav class="navbar navbar-dark bg-dark">
      <a class="navbar-brand" href="/">yubin cafe</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample01" aria-controls="navbarsExample01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarsExample01">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
          </li>
          <% unless user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Sign In', sign_in_path, class: 'nav-link'%>
          </li>
          <% else %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="https://example.com" id="dropdown01" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><%= current_user.user_name %></a>
            <div class="dropdown-menu" aria-labelledby="dropdown01">
             <%= link_to 'Sign Out', sign_out_path, method: 'delete', class: 'dropdown-item', data: {confirm: '로그아웃 하시겠습니까?'} %>
            </div>
          </li>
          <% end %>
        </ul>
        <form class="form-inline my-2 my-md-0">
          <input class="form-control" type="text" placeholder="Search" aria-label="Search">
        </form>
      </div>
    </nav>